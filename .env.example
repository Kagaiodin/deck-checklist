# Google Drive integration (Tier 2, issue #52) — client-side OAuth only, no secret.
# Create an OAuth 2.0 "Web application" client ID in Google Cloud Console, with
# this app's origin under "Authorized JavaScript origins" (no redirect URI needed —
# Google Identity Services handles the popup). Leave unset to hide the feature.
VITE_GOOGLE_CLIENT_ID=
